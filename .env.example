# Neon console: copy the pooled PostgreSQL connection string, including TLS options.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
PORT=4173
# Set true to enable location permission requests and live weather. Restart after changing.
REALTIME_WEATHER_ENABLED=false
