{
  "name": "bonsai-growth-lab",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.9"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
